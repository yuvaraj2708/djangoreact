import * as mutationsSelectors from './selectors';
import { optimisticMutationsHor } from './reducer';
import { ActionCreators, CreateEffectConfig, Mutations, ReducersMap, Reducer } from '../types';
export declare function createMutationsRjObjectEnhancer<M extends Mutations>(mutations: M): {
    actionCreators: <A extends ActionCreators>(actionCreators: A) => A & ({} extends { [k in keyof M]: import("../types").MutationActionCreator; } ? { [k in keyof M]: import("../types").MutationActionCreator; } : { [k in keyof M]: import("../types").MutationActionCreator; });
    enhanceRootReducer: (reducer: Reducer, actionCreators: ActionCreators) => Reducer<any, import("../types").Action<string>>;
    enhanceReducer: typeof optimisticMutationsHor | undefined;
    reducersToCombine: ReducersMap<any>;
    selectors: typeof mutationsSelectors;
    makeObservables: (sideEffect: CreateEffectConfig) => import("../types").MakeRjObservable[];
};
