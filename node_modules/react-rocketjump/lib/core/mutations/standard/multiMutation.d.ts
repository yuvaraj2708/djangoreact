import { Mutation, MutationReducer } from '../../types';
declare type KeyMaker = (...a: any[]) => any;
export interface MultiMutationState {
    errors: Record<string, any>;
    pendings: Record<string, boolean>;
}
export default function multi<M extends Mutation>(makeKey: KeyMaker, mutationConfig: M): Mutation<MutationReducer<MultiMutationState>, M extends Mutation<any, infer H> ? H : any, M extends Mutation<any, any, infer H> ? H : any>;
export {};
