import { Mutation, MutationReducer } from '../../types';
export interface SingleMutationState {
    pending: boolean;
    error: any;
}
export default function singleMutation<M extends Mutation>(mutationConfig: M): Mutation<MutationReducer<SingleMutationState>, M extends Mutation<any, infer H> ? H : any, M extends Mutation<any, any, infer H> ? H : any>;
