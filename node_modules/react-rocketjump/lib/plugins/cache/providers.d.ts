import { CacheStore } from './stores';
export interface CacheProvider {
    readonly namespace: string;
    readonly size: number;
    readonly store: CacheStore;
    has(key: string): boolean;
    get(key: string): any;
    set(key: string, value: any): any;
    clear(): void;
}
export declare class LRUCache implements CacheProvider {
    readonly namespace: string;
    readonly size: number;
    readonly store: CacheStore;
    constructor(namespace: string, size: number, store: CacheStore);
    _effectiveKey(key: string): string;
    _meta(): any;
    _setMeta(nextMeta: any): void;
    _resetMeta(): void;
    has(key: string): boolean;
    get(key: string): any;
    set(key: string, value: any): void;
    clear(): void;
}
export declare class FIFOCache implements CacheProvider {
    readonly namespace: string;
    readonly size: number;
    readonly store: CacheStore;
    constructor(namespace: string, size: number, store: CacheStore);
    _effectiveKey(key: string): string;
    _meta(): any;
    _setMeta(nextMeta: any): void;
    _resetMeta(): void;
    has(key: string): boolean;
    get(key: string): any;
    set(key: string, value: any): void;
    clear(): void;
}
