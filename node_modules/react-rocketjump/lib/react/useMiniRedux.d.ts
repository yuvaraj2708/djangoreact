import { Reducer as ReactReducer } from 'react';
import { Action, DispatchFn, MakeRjObservable, RjEffectPipeliner } from '../core/types';
import { DebugInfo } from '../debugger/types';
export default function useMiniRedux<T>(reducer: ReactReducer<NonNullable<T>, Action>, makeObservable: MakeRjObservable, pipeActionStream: RjEffectPipeliner, debugInfo: DebugInfo): [NonNullable<T>, DispatchFn];
