import { ReactNode } from 'react';
import { AuthRoutesConfig } from './AuthRoutesContext';
export declare type AuthRoutesConfigProps = {
    children: ReactNode;
} & AuthRoutesConfig;
export default function AuthRoutesProvider({ children, ...props }: AuthRoutesConfigProps): JSX.Element;
