import { ComponentType, ReactNode } from 'react';
import { RouteProps } from 'react-router-dom';
export declare type MaybeAuthRouteProps = {
    spinner?: ReactNode;
    spinnerComponent?: ComponentType;
} & RouteProps;
/**
 * Wrapper around `<Route />`, render given spinner or null while auth is loading
 * then render given content for both guest and authenticated user.
 *
 */
export default function MaybeAuthRoute({ children, component, render, spinner: localSpinner, spinnerComponent: localSpinnerComponent, ...rest }: MaybeAuthRouteProps): JSX.Element;
